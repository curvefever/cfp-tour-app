# Curve Fever Pro Tour Hub — Project Handoff Brief

## What this project is
A web app for setting up and running Curve Fever Pro tournaments — organisers load a roster, generate rounds, enter scores, resolve ties, and manage players/teams live during an event; a shareable live link lets everyone else follow the scoreboard, bracket, and rankings as it happens. It replaced a fragile Google Sheets workflow, then (as of **2026-09-10**, see "The rework" below) was itself replaced by a full React/TypeScript rebuild — the original vanilla-JS/HTML app is preserved read-only under `legacy/` and is still the best reference for pre-rework build history and design rationale (see "Using `legacy/` and the pre-rework log" below).

---

## About these files
`HANDOFF.md` (this file) covers **current state only** — what the app does today, its rules, what's still open, and what's next. Detailed, dated write-ups of every individual build live in **`HANDOFF_LOG.md`** instead, in the same folder — kept separate so a fresh session only has to read this file in full to get oriented, and can `grep`/search the log for a specific past decision on demand rather than reading it start to end. A third file, **`ROADMAP.md`**, holds forward-looking assessments of projects raised but not yet started (scope, feasibility findings, ordering, open questions) — distinct from both of the above, since it's neither current state nor a completed build; an entry moves to `HANDOFF_LOG.md` (with a pointer added here) once actually built. All three files are tracked in the repo (as of 2026-09-17 — previously gitignored, local-only working docs, switched over so they're versioned alongside the code). Process/workflow rules (how to work, not what's built) now live in `AGENTS.md` under "Working process" — `CLAUDE.md` was retired 2026-09-10 once its content was folded in there.

---

## Plan before coding (organiser's standing rule, 2026-09-18)
Every new feature or significant addition/change starts with a plan (Plan Mode), which the organiser reviews and approves **before any code is written**. Only small, low-risk changes (a one-line fix, a doc edit, a trivial tweak) can be made directly, without a plan first. If in doubt about whether something counts as "small," treat it as not small and plan first.

---

## The rework (2026-09-10)
The entire app was rebuilt from scratch as a React/TypeScript codebase and merged via PR #1 ("rework", `faea942`), landing 28 commits in one go. The old vanilla-JS app was moved to `legacy/` rather than deleted. This was **not a redesign** — it was a faithful, thorough port: every game format, pooling phase, bracket phase, and viewer-facing feature documented in this file's pre-rework history was carried over, including subtle formula-level details (e.g. `fairPoints(rank, score) = rank - score / 100_000`, the corrected tie-break sign from a legacy-era fix, and the multi-game Semis key scheme `r{ri}-rm{rm}-p{pi}-g{g}[-m{mi}]` — both confirmed by reading the new `src/domain/tournament/scoring.ts` directly, not assumed from commit messages). Two things did change materially, on purpose, not as ported behavior:
1. **Admin access.** The old single-shared-password model is gone. The new app authenticates against real Curve Fever Pro accounts (email/password login via the CFP API) and authorizes by account role (`ADMIN`/`MOD`/`LEAD_MOD`/`DEVELOPER`/`TOUR_HOST`/`LEAD_TOUR_HOST`) — see "Admin access" below.
2. **Live-sync writes.** The browser no longer writes straight to Firebase. A write now goes through a server function that re-validates the caller's admin session on the server before touching Firebase — see "Sync & persistence" below.

Deliberately **not** part of the rework: `last-man-standing` and `kings-valley` remain unimplemented (they were "coming soon" placeholders in the legacy app too — not a regression, just still-open work). No new game format, pooling phase, or bracket phase was added beyond legacy parity.

### Using `legacy/` and the pre-rework log
If a bug shows up post-rework, or a rule in this file seems underspecified, check the equivalent implementation in `legacy/js/*.js` — the rework aimed for behavioral parity, so the old code is a legitimate reference for "what should this actually do," not just historical trivia. Correspondingly, **every `HANDOFF_LOG.md` entry dated before 2026-09-10 describes the legacy vanilla-JS app**, not the current one — still useful for the *reasoning* behind a rule (why lucky-loser selection works the way it does, why Fair Points' sign was flipped, etc.), but any file path or function name in those entries refers to `legacy/js/*.js`, not `src/`.

---

## Current architecture
- **Stack**: React 19 + TypeScript, TanStack Start (SSR, file-based routing via TanStack Router) + Vite, Tailwind CSS v4, Firebase Realtime Database, `pnpm`. First build tooling this app has ever had — the legacy app had no build step, no linter, no test runner at all.
- **Domain layer** (`src/domain/tournament/*.ts`): the tournament rules engine, ported to pure functions over a typed `TournamentState` (replacing the legacy untyped global `T`), with an injectable `TournamentRuntime` (clock/random/ID source) for determinism. One file per concern — `types.ts` (state shape), `formats.ts` (`GAME_FORMATS` registry), `generation.ts`/`schedule-generation.ts` (bracket building), `pooling.ts`, `single-elimination.ts`/`double-elimination.ts`, `advancement.ts` (tie-breaks, `computeRankings`, cutoffs), `scoring.ts` (Fair Points, `getUnitScore`), `seeding.ts`, `finals.ts`, `rankings.ts`, `roster.ts`, `mutations.ts`, `bracket.ts`, `room-distribution.ts`, `transitions.ts`, `state-defaults.ts`, `runtime.ts`.
- **Feature layer** (`src/features/*`): one folder per tab/concern — `admin/` (`SetupView`, `RunningAdmin` + `RunningAdminRoster`/`RunningAdminScores`/`RunningAdminStatus`), `archive/` (`ArchiveView`), `auth/` (`AuthProvider`, `CfpLoginModal`, `auth.shared.ts`, `server/*.server-fns.ts`), `bracket/` (`BracketView`), `rankings/` (`RankingsView`, `rankings-image.ts`), `scoreboard/` (`ScoreboardView`), `shell/` (`AppShell` — tab bar/nav), `sync/` (`firebase-client.ts`, `firebase-admin.server.ts`, `live-sync.ts`, `tournament-write.server-fns.ts`), `tournament/` (`TournamentProvider` — the app-wide state context).
- **Shared UI** (`src/components/ui/*`): `Badge`, `Button`, `Feedback`, `Form`, `Stats`, `Surface`, `Table`, `Timeline` — the legacy app had no component abstraction at all (template strings + DOM manipulation).
- **Persistence** (`src/lib/persistence/*`): still a pure-`localStorage` fallback, reading/writing the same legacy key `curveFFA_state_v1` — the app works fully standalone, no backend, exactly like before, if Firebase/the server is unreachable. Archive (`archive.ts`) is unchanged in spirit: a separate, local-only, Firebase-independent system.
- **Repo-specific conventions**: `.agents/skills/project-structure/SKILL.md` and `.agents/skills/unslop/SKILL.md` describe general TanStack Start/feature-folder conventions this codebase follows (thin routes, `*.server-fns.ts` naming) — not tournament-domain-specific, more like a style guide.

---

## Deployment target
The legacy app deployed as static files to GitHub Pages — no server, no secrets, `git push` to `main` was the whole deploy story. **That no longer works** — the new app's `pnpm start` runs `node .output/server/index.mjs` (a Nitro/TanStack Start server bundle), and admin writes require a live server function with access to `FIREBASE_SERVICE_ACCOUNT_JSON` (or Application Default Credentials) plus the CFP API.

**Decided (2026-09-10), live and confirmed as of 2026-09-17**: hosted on Curve Fever Pro's own infrastructure, alongside `api.curvefever.pro`. `.github/workflows/release-tour.yml` (in this repo) handles CI/CD: a push to `main` or `test` builds a Docker image (pushed to GHCR, tagged `latest`/`test-latest` respectively) and deploys it via SSH over Tailscale to CFP's infra — `main` → prod, `test` → the staging target. Confirmed live URLs:
- **Production** (`main`): **https://tournaments.curvefever.pro/**
- **Test/staging** (`test`): **https://tournaments-test.curvefever.pro/** — this is the address used for all live-browser verification in recent `HANDOFF_LOG.md` entries (DNF/no-show tracking, qualAdv fix, Fair Points averaging, etc.).

Standing branching rule (per Claude's own memory, `project_deployment_branching.md`): push to `test` freely; pushing to `main` (which deploys straight to the real production URL above) always requires the user's explicit authorization for that specific push.

**Still unconfirmed** (not blocking): exactly how `FIREBASE_SERVICE_ACCOUNT_JSON`/ADC reaches the deploy target, and whether `VITE_API_ENDPOINT` needs setting explicitly there — worth asking the CFP developer who set up the pipeline if a Firebase-write or CFP-login issue ever shows up only in the deployed environment and not locally.

---

## Game formats & tournament structure
Full parity with the legacy app (`src/domain/tournament/formats.ts`, `GAME_FORMATS`):
- **`ffa-individual`** — FFA, individual, room size 6–8 (ideal 8).
- **`team-2v2v2v2`** — teams of 2, room size 3–4 (ideal 4).
- **`team-3v3v3`** — teams of 3, room size 2–3 (ideal 3).
- **`team-3v3`** — teams of 3, head-to-head (room size fixed at 2), odd-count strategies `none`/`bye`/`flex`.
- **`individual-1v1`** — head-to-head, odd-count strategies `none`/`bye`.
- **Not implemented** (excluded from `ImplementedGameFormatKey`, shown as disabled "coming soon" in Setup): `last-man-standing` — was already an unimplemented placeholder in the legacy app, not a regression.

**Pooling phases** (`poolingPhase`): `none` (2-round no-elim warm-up), `qual-table` (3 fixed qualification rounds, Fair Points averaged across rounds played), `swiss` (3–7 rounds, fold-pairing, same averaged standings as qual-table — they share one standings engine), `group-stage` (round-robin groups, single or double round-robin, top N per group advance, same averaging) — **both Swiss and Group Stage restricted to head-to-head formats** (`individual-1v1`, `team-3v3`) as of 2026-09-11, gated in both the Setup UI and `generateTournament()`'s validation, since round-robin/fold-pairing always pairs exactly 2 units per room and can't represent a multi-way room shape (FFA/`team-2v2v2v2`/`team-3v3v3`). See "Restrict Group Stage to head-to-head formats" and "Prevent impossible tournament-setting combinations — Stage A" in `HANDOFF_LOG.md`. As of 2026-09-17, a reserve can only be added to a qual-table/Swiss tournament while at most one qualifying round has been completed (`addReserveUnit`, `mutations.ts`) — averaging alone doesn't stop a reserve from winning a spot off a single lucky round, so admission itself is gated instead. See "Fair Points: sum → average, plus a reserve-admission guard for qualification-table/Swiss" in `HANDOFF_LOG.md`. As of 2026-09-18, a qual-table/Swiss tournament can also mark its own leading N rounds as played-but-excluded from the cumulative standings (`excludeFromStandings` on `TournamentRound`, set by `qualificationTablePoolingPhase`/`swissPoolingPhase` via a new "Non-counting rounds (optional)" Setup field) — useful for rounds that exist only to seed the field fairly, not to count toward who advances; `computeQualificationStandings` (`advancement.ts`) skips them, `tieredSeed`'s per-round reseeding is unaffected since it never read the cumulative table anyway. Deliberately scoped to qual-table/Swiss only, not Group Stage. See "Non-counting qualification rounds" in `HANDOFF_LOG.md`.

**Bracket/elimination phases** (`scheduleLogic`): `single-elimination`, `double-elimination` (head-to-head race bracket + grand final), `double-elimination-shared-final` (multi-unit double-elimination feeding one shared Final), **`kings-valley`** (new, 2026-09-11) — a persistent room ladder: rooms ranked top-to-bottom, each round's top finishers in a room promote to the room above, bottom finishers demote to the room below, the middle stays, and only the *bottom* room's bottom finishers are actually eliminated. Room counts/sizes are recomputed each round from the shrinking survivor count via the same `distributeRooms()` every other schedule uses; promotion/demotion band sizes are a fixed fraction of room size (`KINGS_VALLEY_MOVE_FRACTION = 0.25`), with a larger fraction (`KINGS_VALLEY_ELIMINATION_FRACTION = 0.5`) concentrated in the bottom room specifically so the format still converges to a Final in a bounded number of rounds (capped at `MAX_KINGS_VALLEY_ROUNDS = 14`) despite only ever eliminating from one room per round. Works for every implemented game format — no compatibility gate, no new Setup field (the organiser tunes nothing, matching "One important principle" below). See "Kings Valley" in `HANDOFF_LOG.md`. **`waterfall-bracket`** (new, 2026-09-21, supersedes `ROADMAP.md` items 7 & 8) — an entirely organiser-authored, generation-time-only rank-band routing bracket: the organiser types a `ROUNDS:`/`ROUTES:` mini-language into a Setup textarea (`src/domain/tournament/waterfall-bracket.ts` parses/validates it) describing an arbitrary round graph where each room's finishers split by rank into bands, each band routing to its own destination round — not necessarily the next one, and not necessarily all to the same place, so a room's own top-4 can skip several rounds straight into a semifinal room while its 5th–8th feed a consolation room several other rooms also feed into. No live reseeding, no bye/lucky-loser handling, reserves blocked entirely once the tournament starts. Known live-verified UX characteristic, not a bug: Rankings can transiently show an actually-still-alive unit as "eliminated" for one round, whenever its real destination round hasn't been finalized yet — self-corrects the moment that round is actually reached; identical, pre-existing timing to double-elimination's own deferred-LB-target case. See "Waterfall / rank-band bracket phase" in `HANDOFF_LOG.md`.

**Semis/Final**: both support multi-game scoring (1–4 games, summed) via the "Finals format"/"Semis format" setup fields — same underlying mechanism as legacy (Final: `T.finalScores`/flat pool; Semis: `T.scores` extended with a `-g{n}` key segment, room-based).

**Scoring**: two systems, organiser-selectable per tournament whenever `poolingPhase !== 'none'` (`ScoringSystemKey = "fairpoints" | "positional-points"` — "Raw score only" was already removed pre-rework and stayed removed). **Fair Points** (default): `fairPoints(rank, score) = rank - score / 100_000` — lower is better, a higher score wins a same-rank tie (confirmed directly in `src/domain/tournament/scoring.ts:150-152`); cumulative `totalFP` is *averaged* across rounds played. **Positional Points** (new, 2026-09-20, `ROADMAP.md` item 5): an organiser-supplied fixed rank-to-points table (`positionalPointsTable`, comma-separated, highest rank first, e.g. "10,8,6,5,4,3,2,1", validated to cover the format's largest possible room size and to never increase rank-to-rank) — higher is better, and cumulative points are *summed* not averaged, matching a real organiser spreadsheet's own convention (summing a per-round value that's already bounded at 0 naturally penalizes a smaller sample, unlike Fair Points, which needs averaging for that same reason). Both systems share `materializeStandings`/`computeQualificationStandings`/`computeGroupStandings` (`advancement.ts`) via a single scoring-aware branch — the one subtlety was that Fair Points is lower-is-better while positional points is higher-is-better, requiring a `compareStandingValue()` helper at exactly the two places that hard-code a sort direction (everything else that reads `totalFP` — tie-cutoff detection, dense ranking — turned out to already be direction-agnostic). Deliberately scoped to qual-table/Swiss/Group-Stage standings only — Semis/Final have no rank-based scoring at all today (raw score sum) and were left out of scope, not half-built. See "Positional-points scoring" in `HANDOFF_LOG.md`.

---

## Room reseeding
`src/domain/tournament/seeding.ts` holds several distinct mechanisms for deciding which room a player/team lands in next round. Documented here in plain language so a future session can reason about "how did this person end up in this room" without re-deriving it from the code:

- **`randomSeed`** — Round 0 only (initial roster → Round 1 rooms). A true shuffle via the injectable `RandomSource`.
- **`swissFoldPair`** — Swiss pooling rounds. Fold-pairs by rank (rank 1 vs. rank `half+1`, etc.), with a rematch-avoidance swap applied when one exists that clears both pairs at once.
- **`sequentialSeed`** — takes an already-ordered flat list and slices it directly into a target round's pre-declared room sizes, with no reordering/bouncing. Used for Kings Valley's every room-to-room ladder movement, including the one-time hop into Kings Valley's first round — chosen there specifically so every room always shows exactly as many slots as its declared size (no stray dash, no silent overflow). As of 2026-09-17, no longer used for Bracket's future-round *display projection* (see `distributeTierMajorPool` below) — see "Fix: future-round Bracket placeholders clustered by rank instead of spreading" in `HANDOFF_LOG.md`.
- **`distributeTierMajorPool`** (new, 2026-09-17) — the mechanism behind Bracket's future-round *display projection* (`projectFutureRoundSlots`), replacing `sequentialSeed` at that one call site. The projection still builds its candidate pool tier-major (every room's own rank-1 finisher, then every room's own rank-2, ...) — fixed 2026-09-15 to stop same-room-shape targets from collapsing to exact same-room carryover — but a plain sequential chop of that pool had its own residual flaw: whenever a target room's size spans more than one source room's worth of a single tier (a small source-room count feeding a large target room), the first target room could swallow two or more whole rank-tiers before the chunk boundary ever reached a lower rank, showing e.g. only Rank 1/2 finishers and no Rank 3+ — reported live on a real tournament. `distributeTierMajorPool` round-robin-assigns each tier's own entries across target rooms with remaining capacity, rotating the round-robin's starting room by one every time the tier changes, so every room gets a spread across ranks *and* source rooms instead of a homogeneous block. Same accepted tradeoff as before (unchanged in spirit): every projected slot still matches its own room's declared size exactly, but the exact target-room assignment can still drift from what the real transition (`tieredSeed`/`tieredBracketSeed`) eventually produces once the round is actually reached. See "Fix: future-round Bracket placeholders clustered by rank instead of spreading" in `HANDOFF_LOG.md`.
- **`snakeSeed`** — the original, simpler room-reseeding mechanism: takes advancing candidates ordered room-major/rank-minor (each room's own survivors best-to-worst, room by room) and distributes them into target rooms via a boustrophedon (zig-zag) bounce. As of 2026-09-12, **not called anywhere in the live app** — both places that used it (the generic transition path, and double-elimination's WB/LB routing) now use `tieredSeed`/`tieredBracketSeed` instead. Kept as an exported, unit-tested primitive rather than deleted.
- **`tieredSeed`** (new, 2026-09-12) — replaces `snakeSeed` at the one call site in `transitions.ts` that drives every *ordinary* room-based transition: no-elim warm-up rounds, Qualification Table, and real single-elimination/team-format cutting rounds, up to and including the round that decides Semis' own room composition. Built to fix two real problems the organiser identified by hand-simulation of `snakeSeed`'s old behavior: (1) **extremes-clustering** — a room's own best and worst finisher could fold together into the very same next room regardless of true global standing; (2) **round-to-round staleness** — the same players kept landing in the same room together round after round once standings stabilized (verified up to 8/8 unchanged room membership by round 5-6 in a 32-player no-elim simulation).

  **How it works, in plain terms**: instead of listing survivors room-by-room, `tieredSeed` groups them into *rank tiers* spanning every room at once — tier 0 is every room's own winner, tier 1 is every room's own runner-up, and so on. (A room's own within-room dominance margin, `pct`, is used only to break a tie *within* the same tier, never to compare across tiers — a room's winner's margin only says something about that room's specific opponents.) It fills the next round's rooms one "wave" at a time (a wave is one tier, or a partial tier when room sizes are uneven), and for each wave it tries every way of assigning that small group of candidates to the available rooms, picking whichever assignment minimizes a weighted cost of (a) recency-weighted repeat-pairs against everyone already placed this reseed and every prior round's history, and (b) how far each room's running total would land from perfectly balanced. A pairing that last shared a room the round before gets a mild ~10% cost premium over one that hasn't shared a room in many rounds — enough to nudge the search away from the very freshest repeats without ever letting that override balance. The diversity-vs-balance weighting itself shifts smoothly over the bracket: heavily diversity-weighted during no-elim warm-up and the earliest real cutting rounds, shifting to heavily balance-weighted by the round that seeds into Semis — so a spot in the Final stays decided by skill and results, not by forcing together players who simply haven't played each other yet. (The Final itself is always a single room, so there's nothing left to taper by that point.)

  A new persisted state field, `roomHistory: Record<string, number>` (a canonical pair-key → the round index that pair most recently shared a room), tracks match history across the whole tournament — mirrors the existing `poolingByeCounts` field's shape and update pattern.

  **Scope, explicit**: only the one `transitions.ts` call site described above. Kings Valley's own ladder movement (`sequentialSeed`, not a global reseed at all) is untouched by this change.

- **`tieredBracketSeed`** (new, 2026-09-12) — the same diversity/recency/taper mechanism extended to double-elimination's WB/LB routing (`finalizeDoubleEliminationRound`, both the `race` and `shared-final` variants — the one remaining `snakeSeed` call site). Structurally different from `tieredSeed` because a target round's pool there can accumulate from **more than one source round** (a losers-bracket round routinely pools a WB round's fresh drops together with the previous LB round's own survivors) — `buildAdvancementTiers` only ever handles one source round at a time. Resolved by tagging each contribution's room-rank tier at the moment it's pushed (`advanceDoubleElimination`, one source round at a time, while a single `buildAdvancementTiers` call is still valid), so by the time the pool is finalized every entry already carries a directly comparable tier number regardless of provenance. Uses a new taper, `doubleEliminationApproachProgress` — computed **per bracket side** (WB and LB independently approach one shared terminal round, unlike the generic path's single linear ladder), with the terminal round (Grand Final, or the shared-final variant's untagged terminal Final) always at progress 1. `assignWaveToRooms`/`recencyWeight`/`roomHistory`/`recordRoomHistory` are all reused completely unchanged — `roomHistory` is one global map, so a WB-round rematch is already correctly visible to LB-round avoidance with no extra wiring. Double-elimination rounds now write to `roomHistory` too, closing a gap where they previously never did. See "Extend diversity + rematch-aware reseeding to double-elimination's WB/LB routing" in `HANDOFF_LOG.md` for the full design/testing account, including one disclosed, accepted test-coverage gap (the `targetRoundIndex` convention at this call site is verified by direct reasoning, not a dedicated adversarial mutation test).

  See "Diversity + rematch-aware room reseeding" in `HANDOFF_LOG.md` for the full design rationale, algorithm derivation, and testing account.

  **Behavior worth knowing when explaining this to players** (found 2026-09-21 by running the real `assignWaveToRooms` on synthetic brackets — see "Seeding system explainer + seeding-behavior findings" in `HANDOFF_LOG.md`): the balance term is often an exact tie — whenever every candidate in a wave shares the same tier rank, and for *any* two-room destination starting from zero totals — so the automatic taper acts almost identically to pure `diversity` in common bracket shapes; balance visibly bites only in reseeds with 3+ destination rooms. And a pure `balance` override (diversityWeight exactly 0) ignores repeat history entirely (ties fall back to permutation order) rather than merely weighting it down, so it can produce *more* repeat opponents than a balance-heavy-but-nonzero blend.

- **Fixed-draw scheduling** (new, 2026-09-20, `src/domain/tournament/fixed-draws.ts`) — an entirely separate mechanism from everything above, used only when a qual-table/Swiss tournament opts into `gamemodeConfig.drawPublication === 'fixed'` (Setup's "Draw publication" field, default `'adaptive'`). Computes the *whole* room schedule once at generation time from roster order alone — never from live results — instead of reseeding round to round. Swiss reuses `circleMethodSchedule` (the same pairwise round-robin primitive Group Stage's own schedule already uses) directly, guaranteeing zero repeat pairings. Qual-table (room size can exceed 2) uses a new greedy wave-based fill reusing `assignWaveToRooms` — the same room-choice primitive `tieredSeed` itself uses — with every candidate's tier rank held constant (there's no live skill signal to balance in a fixed draw), matching `tieredSeed`'s own accepted quality bar rather than exceeding it. The result is stored per-round on `TournamentRound.fixedRoomAssignments`, not pre-populated into `state.assignments` (which is used elsewhere as the "this round has been reached" signal — see `rankings.ts`'s `lastAssignedRound`) — mirroring Group Stage's own `matches`/`groupByes` precedent exactly. `transitions.ts` reads it directly when present, bypassing `tieredSeed`/`swissFoldPair` entirely; `roomHistory`/`poolingByeCounts` for the whole fixed schedule are folded in at generation time so the live transition out of the pooling phase still avoids rematches against the fixed rounds. See "Pre-published, fixed multi-round draws" in `HANDOFF_LOG.md`.

---

## Admin access
Real per-user authentication against Curve Fever Pro accounts, replacing the legacy single-shared-password model entirely.
- **Login**: `CfpLoginModal` collects email + password, `AuthProvider.login()` calls the CFP API's `/auth/login` (`src/lib/api.ts`).
- **Authorization**: `auth.shared.ts` defines account roles and an allowlist `TOUR_ADMIN_ROLE_IDS = [ADMIN, MOD, LEAD_MOD, DEVELOPER, TOUR_HOST, LEAD_TOUR_HOST]`; `canAdminTour(roles)` checks the logged-in account's roles against it.
- **Session**: access token stored in a cookie + `localStorage` (30-day max age). On every SSR request, `getInitialAuth` (`server/auth.server-fns.ts`) re-validates the token against the CFP API (60s in-memory cache) and only returns an authenticated snapshot if `canAdminTour()` passes.
- **Server-side enforcement**: every privileged write calls `requireTourAdminPermission()` (`tour-permissions.server.ts`), which re-derives the caller's role from their session cookie **on the server**, fresh, every time — not trusted from client-reported state.
- Legacy `localStorage` flags (`curveFFA_admin_unlocked`, `curveFFA_admin_proof_hash`) are actively deleted on hydrate; nothing from the old scheme survives.

**Read access to live tournament data stays fully public** (`database.rules.json`: `tournaments/$id` → `.read: true`) — same model as legacy (a shareable link *is* the access boundary for viewers); only writes got the auth upgrade. Worth keeping in mind — it means a guessed/leaked tournament ID can be read by anyone, unauthenticated, same as before.

---

## Sync & persistence
- **Writes**: browser calls `writeTournament()` (`tournament-write.server-fns.ts`, a TanStack Start server function) → validates the tournament ID, strips any legacy `adminProof` field, calls `requireTourAdminPermission()`, then writes via the Firebase **Admin** SDK (service account) — `database.rules.json` denies direct client writes outright (`.write: false` everywhere). This is the one structurally new piece: legacy wrote straight from the browser, gated only by a DB security-rule hash check.
- **Reads**: still direct client-side Firebase reads (`firebase-client.ts`, `onValue`), public/unauthenticated — a viewer link needs no login, matching legacy.
- **`SyncCoordinator`** (`live-sync.ts`): otherwise architecturally the same as legacy — writer/viewer mode split, debounced push (400ms default), the same null↔sentinel marshal/unmarshal to survive Firebase's array-null-pruning bug (legacy fix, ported), dirty-key tracking so an in-flight remote update can't clobber a field being actively typed.
- **Fallback**: fully standalone if Firebase is unreachable — `getFirebaseSyncTransport()` returns `null` on failure, UI shows an explicit "Live sync unavailable" state rather than breaking; `TournamentProvider` hydrates from `localStorage` (`curveFFA_state_v1`) regardless.
- **Client-side Firebase config** (`firebase-client.ts`) is hardcoded, not env-sourced — normal for Firebase's client SDK (not a secret key), but worth knowing so it's never mistaken for a leaked credential.

---

## Views / tabs
All five legacy tabs exist, and the port looks feature-complete against the legacy `HANDOFF.md` feature list (verified by direct source read, not assumed):
1. **Setup** (`SetupView.tsx`) — game format, schedule logic, pooling phase, odd-count strategy, team scoring rule, Finals/Semis/Grand-Final game counts and size overrides, LB-qualifier count, a "↺ Clear roster" action (roster only, keeps Setup config — see `HANDOFF_LOG.md`), plus a schedule preview timeline before confirming/starting.
2. **Admin / Running Admin** (`RunningAdmin.tsx` + `RunningAdminStatus.tsx`, plus an always-visible "Admin Access" panel in `AppShell.tsx`) — as of 2026-09-15 (see "Reorganize Admin" and "Admin cleanup" in `HANDOFF_LOG.md`), deliberately narrowed to: signed-in-as status + sign out; the tournament name field; a read-only "Tournament Settings" recap (`TournamentSettingsRecap` — game format, schedule logic, pooling phase + specifics, odd-count strategy, team scoring, scoring system, finals/semis format); the "Tournament progress" Timeline; and Save-to-Archive/Reset with title-collision and unsaved-changes prompts. Score entry, tie-break resolution, and roster management all moved out to Bracket (4) and Rankings (5) below — Admin no longer has any tournament-progress stat strip (Round/Players/Rooms/Advancing), removed as redundant with Tournament Progress and Scoreboard.
3. **Scoreboard** (`ScoreboardView.tsx`) — as of 2026-09-15 (see "Scoreboard audit" in `HANDOFF_LOG.md`), a pure standings view: the full qualification/Swiss table or per-group Group Stage tables (`TournamentStandings`), whichever applies — the one thing Scoreboard shows that exists nowhere else in this detail (Rankings only shows a compact one-line rank badge). Shows a short explanatory alert instead of a blank tab for tournaments with no pooling-phase standings (pure single/double-elimination, Kings Valley). No longer shows a per-round room-by-room score table — that was a lossy, buggier duplicate of Bracket's live current-round view once score entry moved there; still admin tooling, not a "viewer destination," per the legacy design notes (see "Design intent for viewer-facing tabs" below) — that framing was preserved, not just the code. As of 2026-09-16 (see "Scoreboard follow-up audit" in `HANDOFF_LOG.md`), the table's title gains a " (Final)" suffix once the tournament has moved past pooling into its bracket phase, so it's never mistaken for still-live data during a long Kings Valley (or any bracket) tail; the Pos column uses dense/competition ranking (`rankStandings()`, `advancement.ts`) so tied entrants share a rank instead of an arbitrary sequential one, and the table's row order reflects any cutoff-boundary tie the admin has actually resolved (`applyQualCutoffOrder`/`applyGroupCutoffOrder`) rather than an arbitrary pre-resolution order. Rankings' compact pool-rank badge shares the same `rankStandings()` fix.
4. **Bracket** (`BracketView.tsx`) — collapsible rounds (past-round-biased default, manual override for any round); for a double-elimination tournament, `bracketBoxes()` (`bracket.ts`, replacing the short-lived `bracketRowGroups()`, settled 2026-09-18 after two earlier layout attempts) groups a winners-bracket (WB) round with every losers-bracket (LB) round immediately following it into one shared box — LB is a *section inside* WB's own box (a "Winners bracket" sub-heading, WB's rooms, then — only when the wave actually has trailing LB rounds — a thin amber divider, a "Losers bracket" sub-heading, and each LB round's rooms stacked, with a small "Round N" micro-label per LB round whenever a box holds more than one), not a separate row or column at all. The box's own top-level label is a plain, continuous "Round N" (reusing `roundNum`, the same field pre-bracket rounds are labeled with), not a WB/LB-specific counter. Every other format (single/no bracket, Kings Valley, pooling-only) still renders as a single row of ordinary single-round boxes, unchanged, by construction. See "Bracket view: merge LB into WB's own box" (and its two superseded predecessors, "Bracket view: stack WB above LB" / "Bracket view: revise WB/LB row layout") in `HANDOFF_LOG.md`. "Follow a player/team" search with auto-scroll (now via native `scrollIntoView`, so it brings the correct row into view first, then centers horizontally within it) to their current round, inline score entry for the current round (multi-game rounds included, via per-game tabs + a "Total" tab — moved here from Admin 2026-09-15), red "⚠ TB?" unresolved-tie badges alongside the resolved "⚖ TB" badge, admin-only "⚠ Tie-break required" resolution cards at the top of the page (moved here from Admin 2026-09-15, so resolving one no longer requires leaving Bracket), a lucky-loser disclaimer (plus live "who's leading" standings for the current round) on any round with a nonzero lucky-loser count, and future-round slots labeled with their projected room of origin instead of a bare dash — real pairings for group-stage (already known at generation time), "Winner of Room N"/"★ Lucky loser (any room)" for single/double-elimination (including no-elim/warm-up predecessors, which contribute a real per-room-rank projection too, not just a dash — fixed 2026-09-12), round-identity labels ("Dropped from WB Round N") for double-elimination losers-bracket edges, a uniform "Qualifier from standings (seed TBD)" for pooling-phase cutoffs, falling back to a plain dash (and poisoning anything downstream) wherever the projection can't be resolved ahead of time — see "Bracket transparency" in `HANDOFF_LOG.md`. Every projected room's slot count always matches its own displayed size exactly (`distributeTierMajorPool`-based chunking, fixed 2026-09-12, pool built tier-major since 2026-09-15, round-robin-with-rotating-offset distribution since 2026-09-17 replacing a plain sequential chop — see "Room reseeding" above), so a projected room's contents are spread across multiple real source rooms *and* across the full range of ranks instead of collapsing to same-room carryover or a same-rank block — **note the accepted tradeoff** (unchanged in spirit): the real transition seeds via `tieredSeed` (diversity + recency-aware, see "Room reseeding" above) for ordinary rounds or `tieredBracketSeed` for double-elimination's WB/LB routing, so a projected room's specific contents can still drift from what actually lands there once the round is reached; see "Live-testing pass", "Fix: future-round placeholders show same-room carryover...", and "Fix: future-round Bracket placeholders clustered by rank instead of spreading" in `HANDOFF_LOG.md`. Kings Valley rounds get their own promote/stay/demote/eliminate row coloring and a per-room disclaimer explaining the mechanism; future Kings Valley rounds deliberately show only room sizes, not a per-slot projection (the room-ladder's persistent-membership model doesn't fit the existing projection algorithm) — see "Kings Valley" in `HANDOFF_LOG.md`. Rooms are displayed as letters everywhere (Room A, B, C...) via `roomLetter()` (`bracket.ts`), and a future-round placeholder tags its source round's own display number alongside the room letter (e.g. "Room 2A, Rank 1") so a lettered room reference stays unambiguous across rounds — see "Comfort: letter room names, round-aware future-round placeholders" in `HANDOFF_LOG.md`. The underlying room data stays a plain 1-indexed number everywhere it's used for logic (filtering, score keys, indices); only display is lettered. `RoundBody`'s live green-"advance"/red-"eliminate" row coloring, as of 2026-09-15, correctly reflects the real cross-round standings cutoff on the last qual-table/Swiss/group-stage round specifically (via `computeStandingsCutoffAdvancing()`, `advancement.ts`) rather than showing "everyone advances" — see "Scoreboard audit: trim to standings-only, fix a shared pooling-cutoff bug" in `HANDOFF_LOG.md`. A plain (non-grand-final) Final round can have individual games flagged anonymous — a "🎭 Anonymous matches" sibling card next to the Final's own box lets the flagged game(s) be scored under auto-generated `Finalist-N` placeholder names instead of the real finalists'; once every game in the Final is filled (real or placeholder) a "🔓 Connect & reveal" action merges a placeholder's scores onto its real finalist and reveals the mapping, all in one step. See "Anonymous Finals matches, v1" in `HANDOFF_LOG.md` for the full design (including why grand-final/race Finals and team formats are excluded). Future-round placeholder labels ("Room 6A, Rank 1", "Dropped from WB Round 6") consistently use each source round's own `roundNum` — the same number its own box header shows — not a separate WB/LB-specific counter; `bracketRoundLabels()` (`bracket.ts`) is the single source of truth both the box headers and this content read from. For the FFA/team shared-final double-elimination variant specifically, a losers-bracket round is now guaranteed within at most 2 winners-bracket rounds of a unit's elimination (`MAX_WB_ROUNDS_BEFORE_LB`, `double-elimination.ts`) — previously the room-size-driven target math could defer indefinitely, sitting a dropped player out for 2+ full WB rounds before their first LB match; the forced round, when needed, still makes a genuine cut, just possibly in a smaller-than-ideal room. See "Double-elimination: round-numbering mismatch + losers-bracket round delay" in `HANDOFF_LOG.md`.
5. **Rankings** (`RankingsView.tsx` + `RankingsRoster.tsx`) — still-active section with live pooling-rank badges, pre-schedule roster-only view once a roster is loaded but before generation, finalist/eliminated sections, PNG export (`rankings-image.ts`). As of 2026-09-15 (moved from Admin, see "Reorganize Admin" in `HANDOFF_LOG.md`), also owns roster/team management when unlocked and not a viewer: `ManageRoster` (rename/swap/remove individuals or teams, designated-defender toggle, vacant-slot fill from reserves, duplicate individual/team names hard-blocked everywhere a name can be entered — team member names excepted, by design) and `ReservePanel` (reserve window with smallest-room placement) — matching the organiser's framing of Rankings as the tab meant to own the whole player-info lifecycle, from a simple pre-tournament list through to a true live ranking. As of 2026-09-16 (see "DNF / no-show tracking for removed and swapped-out roster units" in `HANDOFF_LOG.md`), "Final standings" also lists any removed/swapped-out unit that played at least one match as "DNF" (no rank number, appended last, via `state.withdrawnUnits`) — a unit removed/swapped before playing anything never appears in this public view at all, only in a new Admin-only "No-shows" panel (rendered wherever `ManageRoster` already is). A withdrawn unit is guaranteed to never appear in the "Still in tournament" list either, even a ghost name lingering in a group-stage round that was pre-generated before the withdrawal.
6. **Archive** (`ArchiveView.tsx`) — import/export (single entry or full bundle), id-collision handling (overwrite/new/skip), per-tournament annotations, read-only detail view reusing the same Bracket/Rankings renderers as live (always non-editable regardless of unlock state, by construction — see "Reorganize Admin" in `HANDOFF_LOG.md`).

No stubbed or placeholder view was found in this codebase.

---

## Design intent for viewer-facing tabs
Carried forward unchanged from the legacy app's own design-exploration rounds — still the standard to judge new viewer-facing work against:
- **No live events, no audience, no stream overlay.** Every player plays online, alone, at their own computer.
- **Two real success criteria for a viewer**: (1) **convenience** — instantly seeing, zero clicks, which room you're in and who you're facing; (2) **fun/engagement** — seeing how the rest of the tournament is doing.
- **Scoreboard serves neither** — it's admin tooling, not a viewer destination.
- **No staggered reveal** — every score reaches every connected viewer at the same instant.
- **"Follow a player," never "who are you."** Anyone — a player following themselves, a spectator, an admin — can follow any name.
- **A collapsed round is a display state, not a data reduction.** Reopening must show the exact same detail the current round's display uses.
- **Future rounds default to fully visible**, not collapsed or faded — a viewer *may* manually fold any round, current/future included, but the app never imposes that by default.

---

## What is NOT yet built / known gaps
- ~~**Deployment target for the reworked app**~~ — decided (2026-09-10): CFP's own infrastructure, auto-deployed on push to `main` — see "Deployment target" above. A few operational details (secrets delivery, staging target, live URL) still need confirming once the CI/CD is actually set up.
- **`last-man-standing`** — never implemented, inherited unchanged from legacy.
- ~~**`kings-valley`**~~ — implemented (2026-09-11), a new persistent room-ladder bracket phase — see "Game formats & tournament structure" above and "Kings Valley" in `HANDOFF_LOG.md`.
- **Partial automated test coverage.** Domain layer: Stage 1 (2026-09-10): `room-distribution.ts`, `seeding.ts`, `scoring.ts`, `single-elimination.ts`, `pooling.ts`, 74 tests. Stage 2 (2026-09-10): `advancement.ts`, `double-elimination.ts`, `transitions.ts`, `finals.ts`, 49 more tests (123 total). Stage 3 (2026-09-10): `generation.ts`/`schedule-generation.ts` end-to-end smoke coverage, 10 more tests (133 total) — all three planned stages of the domain-layer test-coverage plan are done; it's since grown further alongside feature work (285 tests as of 2026-09-18, including 2 new regression tests from the future-round-placeholder rank-clustering fix, 11 new tests from the non-counting-qualification-rounds feature, and a net +2 from `bracketRowGroups`'s 6 tests being replaced by `bracketBoxes`'s 8 across the WB/LB layout work's several revisions — see "Fix: future-round Bracket placeholders clustered by rank instead of spreading", "Non-counting qualification rounds", and "Bracket view: merge LB into WB's own box" in `HANDOFF_LOG.md`). Outside the domain layer: Stage 1 (2026-09-17) added 132 tests covering pure/near-pure logic — `live-sync.ts` (`SyncCoordinator` + its pure marshal/merge helpers), `lib/persistence/{live-state,storage,archive}.ts`, `lib/api.ts`, the rest of `auth.shared.ts`'s untested functions, and a newly-extracted `tournament-write.shared.ts` (the one production-code change: `validateWriteInput` pulled out of `tournament-write.server-fns.ts` so it's testable without dragging in the Firebase Admin SDK). **554 tests total** (17 more from "Pre-published, fixed multi-round draws" — see "Immediate next priorities" #39 below — then 45 more from "Waterfall / rank-band bracket phase", #40; both in `HANDOFF_LOG.md`). A first Playwright e2e smoke test also exists now (`playwright.config.ts`, `e2e/smoke.spec.ts`, 3 tests, run via `pnpm run test:e2e`), covering the anonymous-viewer shell/tab-switching/login-wall-exists paths, runnable against either a local `pnpm dev` (default) or a real deployed environment (`PLAYWRIGHT_BASE_URL=<url>`) — verified clean against both `localhost:3000` and the real `tournaments-test.curvefever.pro`. Doesn't (yet) test any authenticated admin action, cover React components with Testing Library, or cover server functions directly — `@testing-library/react`/`jsdom` are installed but still unconfigured. See `AGENTS.md`'s "Testing" section and "Deliberately parked design questions" #5/#7 above. See "Domain-layer test coverage — Stage 1", "— Stage 2", "— Stage 3", "Test coverage outside the domain layer — Stage 1", "Playwright e2e — first test, running locally only", and "Extend Playwright e2e to run against the real test site" in `HANDOFF_LOG.md`.
- ~~**Silent error-swallowing around lucky-loser computation**~~ — fixed (2026-09-10): both catch sites now `console.error` the caught error with round/room context instead of discarding it — see "Immediate next priorities" #4 below and "Tighten the silent lucky-loser catch sites" in `HANDOFF_LOG.md`.
- **Public Firebase read access** — unauthenticated, same as legacy; noted here so it isn't mistaken for an oversight introduced by the auth rework (writes changed, reads didn't).
- **Collapsing a Bracket round column doesn't visually shrink it.** Found 2026-09-18 while live-verifying "Bracket view: revise WB/LB row layout" (`HANDOFF_LOG.md`) — `RoundColumn`'s width className appends `collapsed && 'w-8.5 min-w-8.5'` onto a base `'w-52.5 min-w-52.5 ...'` rather than replacing it; both classes end up in the DOM simultaneously and the browser's CSS cascade resolves the wider one regardless of collapsed state (confirmed via direct `getBoundingClientRect()` measurement, not assumed). The header/content still respond correctly to collapse (rotated label, body hidden) — only the outer column's width doesn't shrink. Pre-existing, not introduced by any change this session; flagged, not fixed, since it's unrelated to the task that surfaced it. A real fix would need the width classes to be mutually exclusive rather than appended (e.g. a ternary, matching the pattern the new `roundColumnWidthClass()` helper already uses for its own purpose).
- ~~**A broken Firebase Admin write path can eventually make the whole UI unresponsive, not just show "sync unavailable."**~~ — fixed (2026-09-10), see "Immediate next priorities" #2 below and "Fix: hung Firebase Admin write freezes the UI" in `HANDOFF_LOG.md`.
- ~~**Impossible tournament-setting combinations**~~ — fixed (2026-09-11), both stages: Stage A (Swiss/Group Stage + multi-way formats, several double-elimination reachability gaps) and Stage B (numeric-input robustness — negative overrides, missing upper bounds, an incomplete override-ordering check). See "Immediate next priorities" #6 below and "Prevent impossible tournament-setting combinations — Stage A" / "— Stage B" in `HANDOFF_LOG.md`.
- ~~**Open question: is `snakeSeed`'s room-reseeding actually equitable across a whole tournament?**~~ — fully resolved (2026-09-12): `snakeSeed` was replaced everywhere it mattered — first for the app's main generic transition path (`tieredSeed`), then for double-elimination's WB/LB routing too (`tieredBracketSeed`, both variants) — with the same diversity-and-recency-aware, taper-based reseeding mechanism. See "Room reseeding" above and "Diversity + rematch-aware room reseeding" / "Extend diversity + rematch-aware reseeding to double-elimination's WB/LB routing" in `HANDOFF_LOG.md`. `snakeSeed` itself is confirmed (repo-wide search) to no longer be *called* anywhere in the live app — it stays exported and covered by its own unit tests, kept as a small, simple, still-correct primitive rather than deleted outright.
- **`cn()` doesn't deduplicate conflicting Tailwind utility classes.** Found 2026-09-18 while fixing a Bracket score-input overflow bug — `cn()` (`src/components/ui/cn.ts`) is a plain string-join, not a Tailwind-merge; when a component's several layers each pass a conflicting same-property utility (e.g. `Input`'s shared `w-full`, then `ScoreInput`'s own default `w-22.5`, then a caller's `w-13`), all of them land in the DOM class list simultaneously and the browser's CSS cascade — not class-attribute order — decides the winner, independently *per property* (confirmed directly: the affected input's width, padding, and font-size each resolved from a *different* one of the three layers, not consistently the last one). Worked around locally for the one reported case only (`compactScoreClass` in `BracketView.tsx` now uses Tailwind v4's `!` important suffix on every conflicting utility, guaranteeing it always wins) rather than fixing `cn()` itself — a true fix (e.g. adopting `tailwind-merge`) is a cross-cutting change touching every one of `cn()`'s 9 call sites' rendered output, out of scope for a same-session bug fix and exactly the kind of change the organiser's plan-first rule (above) is meant to cover. Other places stacking conflicting utility classes through `cn()` may have the same latent bug, just not yet noticed.
- **Anonymous Finals matches don't support grand-final (race) rounds or team formats.** Deliberate v1 scope, not an oversight — see "Anonymous Finals matches, v1" in `HANDOFF_LOG.md`. Grand-final race progression (`progressGrandFinalRace`, `finals.ts`) decides when to open the next game by reading the two real finalists' own score keys directly; a game scored under a placeholder alias instead would stall the race forever, so flagging one is a no-op there. Team-format Finals are excluded because `getFinalUnitScore`'s team branch reads a real `TournamentTeam` object via `buildTeamMap()`, which a placeholder alias never has. Both would need dedicated follow-up work, not a half-built extension of v1.
- **Known, accepted limitation: averaged Fair Points still has residual small-sample sensitivity beyond the reserve-admission cutoff.** As of 2026-09-17 (see "Fair Points: sum → average, plus a reserve-admission guard for qualification-table/Swiss" in `HANDOFF_LOG.md`), a qual-table/Swiss reserve can only be added while at most one qualifying round has been played, guaranteeing they'll play at least two rounds themselves — but averaging still has *no* volume discount beyond that hard cutoff. A unit that's played only 2 rounds (the minimum a reserve can end up with) is compared on perfectly equal footing against one that's played every round, so a small, lucky sample can still outrank a longer, more consistent record if its rate happens to be better. This was discussed explicitly with the organiser and accepted as a known trade-off of the fixed-threshold approach — the alternative (Bayesian shrinkage, blending each entrant's average with the field average, weighted by rounds played) would smooth this out but was rejected as too opaque, with no obviously-correct tuning constant. Revisit only if the organiser raises real cases where this bites.
- **Known, accepted limitation: a removed/swapped player during a fixed-draw tournament can leave a stale name in an already-published future round.** As of 2026-09-20 (see "Pre-published, fixed multi-round draws" in `HANDOFF_LOG.md`) — a fixed-draw qual-table/Swiss tournament's whole schedule (`TournamentRound.fixedRoomAssignments`) is computed once at generation time, the same class of pre-generation Group Stage relied on before `patchFutureGroupStageRounds` was built to keep it in sync with removals/swaps. No equivalent patch mechanism exists yet for fixed draws — `removeRosterUnit`/`swapIndividual`/`swapTeam` don't touch `fixedRoomAssignments` at all. Reserves are already blocked outright for fixed-draw tournaments (a related, separately-found gap — see the same `HANDOFF_LOG.md` entry), but removal/swap themselves are not currently blocked or patched. Explicitly discussed with the organiser and deferred rather than built in the same pass; revisit if it comes up in practice.

---

## Deliberately parked design questions

Genuine "should we build this, and if so how" questions raised during past sessions and explicitly left open — not bugs, not scheduled work. Don't build any of these unprompted; raise them with the organiser first if they come up again. Consolidated here (rather than left scattered across `HANDOFF_LOG.md` entries) specifically so a future session — in this chat or a new one — can find the full set in one place without re-deriving it from the log.

1. **A "presentation mode" for Bracket** — hiding score-entry inputs and the follow/search bar so Bracket could serve as a bigger, simpler shared-screen live view (the role Scoreboard's old per-round table used to serve before the 2026-09-15 audit trimmed it to standings-only). Raised as the cheaper/safer alternative to maintaining a second correctness-sensitive room-status renderer; never requested, never built. See `project_scoreboard_audit_followups.md` item 1 in Claude's memory.
2. **Rankings' compact per-unit rank badge vs. Scoreboard's full standings table** — two different levels of detail for the same "where do I stand" question (Rankings: one-line `#rank · X.XXX FP`; Scoreboard: full Pos/Player/FP/Score/Played table). Both currently judged legitimate at their own fidelity; open question is only whether the organiser ever finds the two views inconsistent or wants them consolidated into one. See `project_scoreboard_audit_followups.md` item 3.
3. **`last-man-standing` game format** — never implemented, inherited unbuilt from legacy (see "What is NOT yet built" above). Not just an unstarted task: its rules/scope were never defined for the reworked pooling-phase/bracket model, so picking this up starts with settling what the format actually means here, not writing code.
4. **Fair Points' residual small-sample sensitivity** — see the "Known, accepted limitation" bullet directly above. Accepted as-is; the Bayesian-shrinkage alternative was considered and rejected as too opaque. Only worth reopening if the organiser raises a concrete case.
5. ~~**Playwright/e2e testing scope**~~ — decided 2026-09-17, done in full for the local + test-site question: `playwright.config.ts` picks its target from an optional `PLAYWRIGHT_BASE_URL` env var (defaults to `http://localhost:3000`, auto-starting `pnpm dev`; a real URL skips the local server entirely). The same 3 tests in `e2e/smoke.spec.ts` run clean against both — verified against the real `https://tournaments-test.curvefever.pro` with zero Cloudflare/bot-protection blocking and no config changes needed, beyond setting the env var: `PLAYWRIGHT_BASE_URL=https://tournaments-test.curvefever.pro pnpm run test:e2e` (bash) / `$env:PLAYWRIGHT_BASE_URL='https://tournaments-test.curvefever.pro'; pnpm run test:e2e` (PowerShell). See "Playwright e2e — first test, running locally only" and "Extend Playwright e2e to run against the real test site" in `HANDOFF_LOG.md`. Item 7 below (authenticating as admin) remains genuinely open and separate.
6. ~~**`auth-session-cache.server.ts`'s testability**~~ — decided 2026-09-17: added the small seam. New exported `resetAuthSessionCache()` (`auth-session-cache.server.ts`) clears the module-level `globalThis` cache Map, so a future direct test of this file can call it between cases instead of reaching into `globalThis` itself. No test file written yet — this only resolves the design question the seam itself doesn't depend on Stage 2 being planned.
7. **How should an e2e test authenticate as admin?** — surfaced while scoping the Playwright smoke test above: this app's admin login is real per-CFP-account auth, re-validated server-side against the real CFP API on every request, with no dev-mode bypass. Testing any admin action (generate a tournament, enter scores, remove/swap a player) needs either real CFP test credentials (a secrets-handling question) or a local Firebase emulator plus app changes (`firebase-client.ts` hardcodes the real production Firebase project today, no emulator switch exists). Neither was decided — raised, not solved, while keeping the first e2e test scoped to what's provable without it (public read-only viewer tabs, and confirming the login wall exists without needing to get past it).

---

## Immediate next priorities
1. ~~**Decide on a deployment target**~~ — done (2026-09-10), fully confirmed live 2026-09-17: CFP's own infrastructure, see "Deployment target" above. Staging target and final URLs confirmed (prod `https://tournaments.curvefever.pro/`, test `https://tournaments-test.curvefever.pro/`); secrets delivery (`FIREBASE_SERVICE_ACCOUNT_JSON`/ADC, `VITE_API_ENDPOINT`) still unconfirmed, not blocking.
2. ~~**Fix: a hung Firebase Admin write eventually makes the UI unresponsive.**~~ — done (2026-09-10): `writeTournamentToFirebase()` (`firebase-admin.server.ts`) now races its `.set(payload)` call against a 10s timeout, so a broken/unreachable Firebase Admin credential fails fast with a clear message instead of hanging forever; `SyncCoordinator.push()` (`live-sync.ts`) also gained an in-flight guard so a slow/failing write can never let requests pile up (a new push while one is still settling is coalesced, not fired concurrently). Verified in-browser: reproduced the original hang (same no-credential dev environment), confirmed the Live Sync card now flips to "🔴 Sync error — … (Firebase write timed out after 10000ms — check FIREBASE_SERVICE_ACCOUNT_JSON / Application Default Credentials.)" within ~10s instead of sticking on "🔄 Connecting…" forever, and confirmed "Next Round" and other interactions stayed instantly responsive throughout, including under a rapid-fire burst of edits. See "Fix: hung Firebase Admin write freezes the UI" in `HANDOFF_LOG.md`.
3. ~~Add domain-layer test coverage~~ — **done (2026-09-10), all 3 stages**: Stage 1 (`room-distribution.ts`, `seeding.ts`, `scoring.ts`, `single-elimination.ts`, `pooling.ts`, 74 tests), Stage 2 (`advancement.ts`, `double-elimination.ts`, `transitions.ts`, `finals.ts`, 49 tests), Stage 3 (`generation.ts`/`schedule-generation.ts` end-to-end smoke coverage, 10 tests) — **133 tests total**, each stage mutation-tested for quality (see "Domain-layer test coverage — Stage 1" / "— Stage 2" / "— Stage 3" in `HANDOFF_LOG.md`). Full staged plan preserved at `C:\Users\thbiaa\.claude\plans\all-right-we-ll-give-humble-stardust.md` under "Domain-layer test coverage" for reference.
4. ~~Tighten the two silent `catch { luckyNames = [] }` sites~~ — done (2026-09-10): investigated first rather than assumed — `roomBasedComputeAdvancement()`'s whole call graph has zero `throw` statements today, so there was no real "expected precondition-not-met case" to preserve, just a defensive catch masking any future bug. Kept the `[]` fallback (no `ErrorBoundary` exists anywhere in the app, so letting it throw could crash the whole live Scoreboard/Admin view) but the catch now `console.error`s the error with round/room context instead of discarding it, matching `TournamentProvider.tsx`'s existing `console.warn` convention for non-fatal errors. See "Tighten the silent lucky-loser catch sites" in `HANDOFF_LOG.md`.
5. ~~Run a real end-to-end playthrough of the reworked app~~ — done (2026-09-10), see "Browser-based full test pass" in `HANDOFF_LOG.md`. Covered a full 37-player FFA tournament (tie-break resolution, cross-room lucky-loser selection, multi-game Final, auto-archive) plus a team-format (3v3v3, 11 teams) generation smoke test — all correct, zero application errors. **Still not tested**: real cross-tab/cross-device live sync against a working Firebase credential (this dev environment had none) — worth doing once the CFP deployment is live and reachable.
6. ~~Prevent impossible tournament-setting combinations~~ — **done (2026-09-11), both stages**: Stage A — Swiss + multi-way format gated (mirroring the Group Stage fix), a stale odd-count-strategy/schedule-logic UI desync closed, two double-elimination reachability gaps (Group Stage with too few qualifiers; an oversized shared-final override) now give clean upfront messages instead of raw deep exceptions, plus `transitions.ts`'s three legacy `!roomSize` throws converted to a proper blocked-result with a try/catch backstop in `RunningAdmin.tsx`. Stage B — six numeric-input robustness gaps closed (negative Semis/Final overrides, an oversized Final override with no Semis override set, Group size's missing upper bound, negative Qualifiers-per-group, negative Grand Final win targets); `qualAdv`'s existing clamp-not-reject behavior deliberately left as-is (already safe, explained in the log). **156 domain tests** (up from 133 at the start of this work), every new guard mutation-tested. See "Prevent impossible tournament-setting combinations — Stage A" / "— Stage B" in `HANDOFF_LOG.md`.
7. ~~Reset roster button + disallow duplicate player/team names~~ — done (2026-09-11): new "↺ Clear roster" action in Setup (roster only, keeps Setup config); duplicate individual player names and duplicate team names now hard-blocked at every entry point (initial load, rename, swap, walk-up add) — closes a real bug where two same-named players silently shared one Finals/Grand-Final score slot. Team member-name duplicates deliberately left unblocked, organiser's explicit call. See "Reset roster button + disallow duplicate player/team names" in `HANDOFF_LOG.md`.
8. ~~Bracket transparency: lucky-loser disclosure + future-round room-of-origin~~ — done (2026-09-11): a static disclaimer (+ live standings for the current round) now explains the lucky-loser mechanism on any round with a nonzero count; future rounds show a projected room-of-origin label per slot instead of a bare dash (real pairings for group-stage; "Winner of Room N" precision for single/double-elimination; round-identity labels for double-elimination losers-bracket edges; a uniform "seed TBD" for pooling-phase cutoffs; graceful fallback — with poisoning of everything downstream — wherever a bye or other live-decided step makes the projection unreliable). 170 domain tests (up from 160), 14 new. See "Bracket transparency: lucky-loser disclosure + projected room-of-origin" in `HANDOFF_LOG.md`.
9. ~~Kings Valley~~ — done (2026-09-11): a new bracket/schedule-logic type implementing a persistent room ladder (promote/stay/demote each round, elimination only from the bottom room), staged as generation → live advancement → Setup UI → rendering, matching the app's own "figure out the balance automatically" principle — no new Setup field, no format-compatibility gate. 187 domain tests (up from 170), 19 new. See "Kings Valley" in `HANDOFF_LOG.md`.
10. ~~Live-test items #6–9 against a real CFP login~~ — done (2026-09-12): found and fixed two real bugs along the way (a Kings Valley first-round room-size mismatch; a projection-poisoning bug that broke Bracket's future-round display for every default-configuration tournament) plus a related display-drift fix (a projected room's slot count could differ from its own declared size) — all three explicitly approved by the organiser before fixing. Item #6 re-verified directly against the real Setup UI with no new findings. 189 domain tests (up from 187). **One open question was raised and deliberately deferred, not resolved**: whether `snakeSeed`'s room-reseeding mechanism actually produces equitable rooms across a whole tournament — see "Parked: is `snakeSeed`'s reseeding actually equitable?" in `HANDOFF_LOG.md` and the "known gaps" bullet below. See "Live-testing pass" in `HANDOFF_LOG.md`.
11. ~~Diversity + rematch-aware room reseeding~~ — done (2026-09-12), resolving item #10's parked question for the app's main generic transition path: a new `tieredSeed` mechanism (per-room-rank tiering, a greedy rematch-aware assignment search, mild recency weighting, a diversity-to-balance taper approaching Semis) replaces `snakeSeed` at the single `transitions.ts` call site driving every ordinary room-based transition. 202 domain tests (up from 189), 13 new, including a multi-seed aggregate statistical test proving fewer cumulative repeat-pairs than the old mechanism, plus live browser verification. Double-elimination's WB/LB routing (still `snakeSeed`) is explicitly out of scope, flagged as important follow-up work. See "Room reseeding" above and "Diversity + rematch-aware room reseeding" in `HANDOFF_LOG.md`.
12. ~~Extend diversity + rematch-aware reseeding to double-elimination's WB/LB routing~~ — done (2026-09-12), closing item #11's own deferred follow-up: `tieredBracketSeed` (new) replaces `snakeSeed` at the one remaining call site (`finalizeDoubleEliminationRound`, both the race and shared-final variants), solving the "pool assembled from more than one source round" problem by tagging each contribution's room-rank tier at push time rather than re-deriving tiers at finalize time. A new per-bracket-side taper (`doubleEliminationApproachProgress`) replaces `semisApproachProgress` for this call site, since WB and LB approach the shared terminal round independently. 211 domain tests (up from 202), 9 new, plus a mutation-testing sanity pass. `snakeSeed` itself is no longer called anywhere in the live app. **Live-verified the same day** against a real CFP login: an 8-player `individual-1v1` double-elimination tournament played through every round to the Grand Final with deterministic scores — LB0/WB1's reseeds matched a hand-derived prediction exactly, and the multi-source "absorb" round correctly avoided re-pairing two players who'd already shared a room back in WB0 (a real, organic — not engineered — confirmation of the cross-bracket `roomHistory` design goal). Zero console errors. See "Room reseeding" above and "Extend diversity + rematch-aware reseeding to double-elimination's WB/LB routing" (including "Live browser verification") in `HANDOFF_LOG.md`.
13. ~~Fix: qual-table rounds reproduce round-0 room membership instead of diversifying~~ — done (2026-09-15): round 0's initial room assignment was never recorded into `roomHistory`, so the first live reseed of any tournament ran `tieredSeed` against an empty history and silently reproduced the prior round's room structure — visible only in qual-table's 3+ consecutive transitions. Fixed generally (any generation-time room assignment), not qual-table-specific. See "Fix: qual-table rounds reproduce round-0 room membership instead of diversifying" in `HANDOFF_LOG.md`.
14. ~~Reorganize Admin: score entry to Bracket, roster to Rankings~~ — done (2026-09-15): narrowed Admin to settings/ties/admin-only info; moved score entry (closing 3 real UI gaps along the way) to Bracket and roster/reserve management to Rankings, added a read-only Tournament Settings recap. See "Reorganize Admin: score entry to Bracket, roster to Rankings, add settings recap" in `HANDOFF_LOG.md`.
15. ~~Fix: future-round placeholders show same-room carryover~~ — done (2026-09-15): `projectFutureRoundSlots` built its pool room-major, which collapsed to exact same-room carryover whenever source/target share a room shape (qual-table's common case); rebuilt tier-major instead, spreading same-source-room candidates across target rooms. See "Fix: future-round placeholders show same-room carryover instead of the real diversified path" in `HANDOFF_LOG.md`.
16. ~~Comfort: letter room names, round-aware future-round placeholders~~ — done (2026-09-15): rooms display as letters everywhere (`roomLetter()`); future-round placeholders now tag the source round's own number alongside the room letter (e.g. "Room 2A, Rank 1") so a lettered room reference stays unambiguous across rounds. See "Comfort: letter room names, round-aware future-round placeholders" in `HANDOFF_LOG.md`.
17. ~~Admin cleanup: drop redundant stat strip, move tie-breaks to Bracket~~ — done (2026-09-15): removed Admin's Round/Players/Rooms/Advancing stat strip (redundant with Tournament Progress + Scoreboard); moved tie-break resolution cards to the top of Bracket, admin-only, so resolving one no longer requires leaving Bracket. See "Admin cleanup: drop redundant stat strip, move tie-breaks to Bracket" in `HANDOFF_LOG.md`.
18. ~~Scoreboard audit: trim to standings-only, fix a shared pooling-cutoff bug~~ — done (2026-09-15): an audit across gamemodes/pooling phases found Scoreboard's own per-round table had four display bugs (Kings Valley mislabeled everyone "Eliminated," the real pooling-cutoff round showed "everyone advances," the grand-final race showed blank scores, Finals never distinguished champion from runner-up) — three of which existed only in Scoreboard's now-duplicate-of-Bracket table, so removing that table (Scoreboard is now a pure standings view) fixed them as a byproduct. The fourth — the pooling-cutoff bug — turned out to also affect Bracket's `RoundBody`, so it got a real root-cause fix there: a new exported `computeStandingsCutoffAdvancing()` (`advancement.ts`) that both consult instead of each hand-rolling a flawed `isNoElim` shortcut. 218 domain tests (up from 211), 7 new. See "Scoreboard audit: trim to standings-only, fix a shared pooling-cutoff bug" in `HANDOFF_LOG.md`.
19. ~~Scoreboard follow-up audit: frozen-standings label + tie-aware ranking~~ — done (2026-09-16): a second audit pass found the standings table stays visible with no indication once pooling ends (misleading during a long Kings Valley tail) — fixed by appending " (Final)" once the current round is no longer a pooling round. Also found the Pos column ignored ties and the display ignored resolved cutoff tie-breaks — fixed with a new exported `rankStandings()` (dense/competition ranking) used by both `TournamentStandings.tsx` and Rankings' identical `poolRanks` bug, plus wiring the previously-unexported `applyQualCutoffOrder`/`applyGroupCutoffOrder` into Scoreboard so a resolved tie reorders the display. 221 domain tests (up from 218), 3 new. Two deeper domain-layer issues (mid-tournament swap drops historical standings; Fair Points summed not averaged) and a real Setup-UI bug (the `qualAdv` field not reliably committing before schedule generation) were found and explicitly parked, not fixed — see "Scoreboard follow-up audit" in `HANDOFF_LOG.md` and `project_scoreboard_audit_followups.md` in Claude's memory.
20. ~~DNF / no-show tracking for removed and swapped-out roster units~~ — done (2026-09-16), resolving item #19's parked "swap drops historical standings" item: standings now never show a removed/swapped-out unit at all (correct per the organiser's explicit spec); Rankings' final standings shows one that played at least one match as "DNF" (no rank number, appended last) and hides a pure no-show from public view entirely, surfacing it only in a new Admin-only "No-shows" panel. New `state.withdrawnUnits` field snapshots display info at the moment of departure (a team's name doesn't survive removal otherwise). Also fixed, found only during live verification: `swapIndividual` (unlike `swapTeam`) never migrated `state.groups[].members`, so a swapped-in individual's own active group-stage performance silently failed to count; `removeRosterUnit` had the identical `state.groups` staleness gap; a removed/swapped unit's old name could still show as "Still in tournament" in Rankings because group-stage's whole schedule is pre-generated upfront. 235 domain tests (up from 221), 14 new, including a new `mutations.test.ts` (zero coverage existed before). See "DNF / no-show tracking for removed and swapped-out roster units" in `HANDOFF_LOG.md`.
21. ~~Setup UI's "Advance to bracket" silent-fallback bug + non-negative-integer score validation~~ — done (2026-09-16), resolving item #19's other parked issue (the `qualAdv` field bug): `generation.ts`'s `parsed()` helper silently substituted `24` for an empty/`"0"` `qualAdv`, with no error shown — now explicitly rejected with a visible error banner, mirroring the existing `semisOverride`/`finalOverride` pattern. Also (raised fresh by the organiser): room/game score inputs now reject negative numbers and decimals at the domain layer (`setRoundScore`/`setFinalScore` in `mutations.ts`) instead of silently accepting negatives or truncating decimals; `0` stays valid. 249 domain tests (up from 235), 14 new. See "Setup UI's 'Advance to bracket' silent-fallback bug + non-negative-integer score validation" in `HANDOFF_LOG.md`.
22. ~~Fair Points: sum → average, plus a reserve-admission guard~~ — done (2026-09-17), resolving item #19's last parked issue: cumulative Fair Points is now an average across rounds played (`materializeStandings`, `advancement.ts`), not a raw sum, so a bye or a late reserve no longer gets an unearned edge purely from a smaller sample — covers qual-table, Swiss, and Group Stage identically via one shared function. Averaging alone wasn't enough for qual-table/Swiss's cutoff mechanism specifically (a reserve could still win a spot from a single lucky round with no volume discount), so `addReserveUnit` now blocks adding a reserve once more than one qualifying round has already been played, for both qual-table and Swiss (they share the same standings engine). Group Stage reserves were already blocked outright for an unrelated reason, unaffected. 257 domain tests (up from 249), 8 new. See "Fair Points: sum → average, plus a reserve-admission guard for qualification-table/Swiss" in `HANDOFF_LOG.md`.
23. ~~Fix: group-stage's pre-generated future rounds aren't updated on removal/swap~~ — done (2026-09-17), closing item 7 from `project_scoreboard_audit_followups.md` (the last open item from the DNF/no-show work): a withdrawn unit's old name used to sit untouched in every future group-stage round's baked-in schedule, becoming a live ghost — a real correctness bug (silently corrupted real players'/teams' Fair Points rank when sharing a room with the ghost), not just cosmetic. Fixed with bye-conversion (a removed unit's future match becomes a bye for its surviving opponent; a swap just relabels old→new) rather than full schedule regeneration — regeneration was rejected because Group Stage's own round count is a live function of group size, and shifting that count would shift every downstream bracket round's absolute index (`winnersTo`/`losersTo`). New `patchFutureGroupStageRounds` helper (`mutations.ts`), called eagerly from `removeRosterUnit`/`swapIndividual`/`swapTeam`. 264 domain tests (up from 257), 7 new. Live-verified for both individual and team group-stage formats. See "Fix: group-stage's pre-generated future rounds aren't updated on removal/swap" in `HANDOFF_LOG.md`.
24. ~~Test coverage outside the domain layer — Stage 1 (pure/near-pure logic)~~ — done (2026-09-17): the real starting state was better than "none" suggested — `@testing-library/react`/`jsdom`/`@playwright/test` were already installed but entirely unconfigured, and `auth.shared.ts` already had 2 tests. Added 132 co-located tests with zero new config: `live-sync.test.ts` (`SyncCoordinator`'s debounce/coalescing/in-flight-guard behavior plus its pure marshal/merge helpers, using `vi.useFakeTimers()`), `lib/persistence/{live-state,storage,archive}.test.ts` (a new shared `createMemoryStorage` fixture; `archive.test.ts`'s `runArchiveImport` coverage is the centerpiece — skip/new/overwrite collision modes, a mid-batch save failure that stops the loop immediately, a swallowed final-index-save failure), `lib/api.test.ts`, and the remaining untested pure functions in `auth.shared.ts`. One production change: extracted `validateWriteInput` out of `tournament-write.server-fns.ts` into a new `tournament-write.shared.ts` (that module top-level-imports the Firebase Admin SDK and `@tanstack/react-start/server`, which would otherwise load into any test that imports anything from it at all) — a behavior-preserving move, verified via a full-suite pass and the server-fn's call site unchanged. Every file mutation-tested (break 1-3 lines, confirm only the intended test fails, revert). **396 tests total.** Two scope questions came up and were explicitly parked rather than decided (see "Deliberately parked design questions" #5/#6 above): Playwright/e2e scope, and whether `auth-session-cache.server.ts`'s module-level cache needs a testability seam. Stage 2 (server functions) and Stage 3 (React components, needs a new jsdom Vitest environment + render-with-providers helper) are sketched but not yet planned in detail — see the plan file referenced in this entry's `HANDOFF_LOG.md` write-up.
25. ~~Resolve the two parked design questions: cache testability seam + Playwright/e2e~~ — done (2026-09-17), following the organiser's own recommendations. Added `resetAuthSessionCache()` (`auth-session-cache.server.ts`), a small test-only seam clearing the module-level cache Map — closes item #6 above. Built the first Playwright e2e test, local-only per the organiser's explicit "local first, extend to the real test site only once proven" plan: `playwright.config.ts` + `e2e/smoke.spec.ts` (3 tests, `pnpm run test:e2e`), covering the anonymous-viewer shell, tab switching, and confirming the login wall exists (opens `CfpLoginModal`) without needing to get past it — real admin auth needs either CFP test credentials or a Firebase emulator, deliberately not solved here (new parked item #7). Found and fixed a genuine hydration-timing gotcha along the way: clicks fired before React attached its listeners were silent no-ops; fixed by waiting for the app's own `data-hydrated="true"` signal before interacting. Also found and fixed a real config gap: Vitest's default file glob also matches `*.spec.ts`, so it was trying to run the Playwright spec itself; excluded `e2e/**` in `vite.config.ts`'s new `test` block (needed a `@ts-expect-error` — vitest 3.2.4 vendors an older, pnpm-nested vite typings version that doesn't structurally recognize `test` on this project's vite@8 `defineConfig`; a real tooling-version gap, not an app bug, confirmed to work correctly at runtime regardless). Also wired `e2e/**/*.ts` into `eslint.config.mjs`'s typed-lint files and `tsconfig.eslint.json`'s `include` (neither covered it before, which would've silently skipped linting it). See "Resolve parked design question: `auth-session-cache.server.ts` testability seam" and "Playwright e2e — first test, running locally only" in `HANDOFF_LOG.md`.
26. ~~Extend Playwright e2e to run against the real test site~~ — done (2026-09-17), the natural next step flagged in item #25 once the local test proved solid, done the same session. `playwright.config.ts` now reads an optional `PLAYWRIGHT_BASE_URL` env var (defaults to `localhost:3000`, auto-starting `pnpm dev`; a real URL skips the local server entirely, since there's nothing local to start). Same 3 tests, zero code changes needed to `e2e/smoke.spec.ts` itself — ran clean against `https://tournaments-test.curvefever.pro` on the first try, no Cloudflare/bot-protection blocking, no flakiness. Confirms the design question is fully closed, not just the local half.
27. ~~Fix: future-round Bracket placeholders clustered by rank instead of spreading~~ — done (2026-09-17), a real bug found live by the organiser on tournament `1789648099343`: Round 3 Room A showed only Rank 1/2 finishers, never Rank 3+, because `projectFutureRoundSlots`'s tier-major candidate pool was chopped sequentially into target rooms — whenever a target room's size spans more than one source room's worth of a single tier (few source rooms, large target room), the first room swallowed whole leading tiers. Display-only — the real transition (`tieredSeed`/`tieredBracketSeed`) was never affected, so no tournament result was wrong, only the predictive placeholder. Fixed with a new `distributeTierMajorPool` (`bracket.ts`), replacing `sequentialSeed` at this one call site with a round-robin-per-tier distribution that rotates its starting room each tier, spreading both rank and source-room within every projected room. 398 domain tests (up from 396), 2 new regression tests (the exact reported shape, plus an uneven-room-size case on the ordinary cutting-round branch). Live-verified against the real tournament's data via a local `pnpm dev` run (the fix isn't deployed yet). See "Fix: future-round Bracket placeholders clustered by rank instead of spreading" in `HANDOFF_LOG.md`.
28. ~~Bracket view: stack WB above LB~~ — done (2026-09-17), **superseded twice, see items 30-31 below** — the row layout described in this entry no longer reflects current behavior; the follow-scroll fix described here is still accurate. The first item off `ROADMAP.md`: a double-elimination tournament's winners-bracket (WB) and losers-bracket (LB) rounds — interleaved in raw array order (`wb1, lb1, wb2, lb2, ...`) — now render as two separate stacked rows instead of one interleaved row, per the organiser's request. New `bracketRowGroups()` (`bracket.ts`) partitions a tournament's rounds into pre-bracket/winners/losers groups (winners includes the terminal Final/Grand-Final, appended last, per the organiser's chosen placement), degenerating to the exact original single-row order for every non-double-elimination format (single-elimination, Kings Valley, pooling-only) — proven by construction, not just tested. `chooseFollow`'s hand-rolled scroll-centering math was replaced with native `column.scrollIntoView({ block: 'nearest', inline: 'center' })`, which now also brings the correct row into vertical view first. 404 tests (up from 398), 6 new. See "Bracket view: stack WB above LB" in `HANDOFF_LOG.md`.
29. ~~Non-counting qualification rounds~~ — done (2026-09-18), the second item off `ROADMAP.md`: a qual-table/Swiss tournament can now mark its own leading N rounds as played-but-excluded from the cumulative Fair Points standings, while `tieredSeed`'s per-round reseeding (already independent of the cumulative table) needed no changes. New `excludeFromStandings` flag on `TournamentRound`, a new "Non-counting rounds (optional)" Setup field (qual-table/Swiss only, not Group Stage), and one new guard clause in `computeQualificationStandings`. 415 tests (up from 404), 11 new. Live-verified end-to-end: a unit that lost badly in an excluded round but won every counted round ranked #1, with the live Scoreboard's "Played" column correctly staying at the counted-round total throughout. See "Non-counting qualification rounds" in `HANDOFF_LOG.md`.
30. ~~Bracket view: revise WB/LB row layout~~ — done (2026-09-18), **superseded the same day, see item 31 below**. A correction to item 28 after the organiser reviewed it live: pre-bracket rounds and WB shared one continuous row instead of each getting their own row, LB moved to be the only other row with spacer-column alignment under WB. Still made LB easy to miss, per the organiser's next review — see item 31. A real, pre-existing, unrelated bug was found and flagged (not fixed) during live verification: collapsing a round column has never actually shrunk its visual width, due to a CSS-cascade quirk in `RoundColumn`'s conditional class list — worked around locally via a new shared `roundColumnWidthClass()` helper (still in use post-item-31) so alignment-sensitive code stays correct regardless. 415 tests, unchanged. See "Bracket view: revise WB/LB row layout" in `HANDOFF_LOG.md`.
31. ~~Bracket view: merge LB into WB's own box~~ — done (2026-09-18), the third and (so far) final attempt at the same underlying request: LB now renders as a section *inside* the same box as its corresponding WB round — "Winners bracket" sub-heading, WB's rooms, a thin amber divider, "Losers bracket" sub-heading, LB's rooms — rather than any separate row or column. `bracketRowGroups()` replaced outright by `bracketBoxes()`, grouping each WB round with every LB round immediately following it in array order (1-2 for the head-to-head/race variant, 0-1 for the FFA/team shared-final variant, one generic scan handles both with no format branching). Box labels reuse the continuously-incrementing `roundNum` field directly ("Round 4", "Round 6"...) instead of a WB/LB-specific counter. Two genuine design forks were put to the organiser rather than decided unilaterally: an empty-LB wave shows no heading (not a placeholder one); two LB rounds stacked in one box each get a small distinguishing "Round N" micro-label, since room lettering restarts per round. Went through Plan Mode first, per the standing rule added the day before. 417 tests (up from 415), 8 new. Live-verified: both double-elimination variants, the real 1-/2-LB-round wave shapes, the degenerate single-elimination case, and collapse-toggling a whole wave box as one unit. Also confirmed (via `git diff --name-only`) that all three attempts at this Bracket layout touch only 3 files total, none overlapping with the unrelated non-counting-rounds feature bundled in the same commit as item 30 — a full revert of the layout work, if ever wanted, would not damage that feature. See "Bracket view: merge LB into WB's own box" in `HANDOFF_LOG.md`.
32. ~~Fix: long usernames overflow the Bracket score input past its box~~ — done (2026-09-18): a single-game, non-team room row's name `<span>` had no `min-w-0`/`flex-1`/`truncate`, so a long, unbreakable name (e.g. "VeryLongUsername") couldn't shrink and pushed its sibling score input off-layout. Fixing that surfaced a second, more serious latent bug in the same input: `compactScoreClass`'s intended `w-13` (52px) width was silently losing to `Input`'s shared `w-full` due to `cn()` not deduplicating conflicting Tailwind classes (see the new "known gaps" bullet above) — the input was actually rendering at ~170px wide, which was the real cause of the overflow, not just the long name. Both fixed: the name span now truncates with a `title` tooltip for the full name, and `compactScoreClass` uses Tailwind's `!` important suffix so its width/padding/radius/font-size always win regardless of `cn()`'s class-order fragility. 417 tests, unchanged (no test coverage gap — this is a rendering/CSS bug, not a domain-logic one). Live-verified: typed "VeryLongUsername" into the roster, confirmed via `getBoundingClientRect()` that the score input's right edge sits well inside the round column's border, and confirmed score entry still works normally. See "Fix: long usernames overflow the Bracket score input past its box" in `HANDOFF_LOG.md`.
33. ~~Anonymous Finals matches, v1~~ — done (2026-09-18), `ROADMAP.md` item 3: a plain multi-game Final can have individual games flagged anonymous, scored live under auto-generated `Finalist-1`/`Finalist-2`/... placeholder names (finalist-order, capped at 10) instead of the real finalists', with a "🔓 Connect & reveal" admin action merging a placeholder's scores onto its real finalist and revealing the mapping in one step, gated to only be possible once every game in the Final is actually filled (real or placeholder) — not `computeRankings()`'s `finalComplete`, which would never become true while a game is still anonymous and unconnected, but a new `isPlainFinalFullyScored()` (`finals.ts`) built specifically for this gate. Grand-final (race) rounds and team formats are excluded, not half-built — see the new "known gaps" bullet above. New `AnonymousFinalist` type, `TournamentRound.anonymousGames`/`TournamentState.anonymousFinalists` fields, three domain mutations (`flagFinalGameAnonymous`/`unflagFinalGameAnonymous`/`connectAnonymousFinalist`, `mutations.ts`), a selector (`anonymousFinalsProgressState`, `finals.ts`), and a new `AnonymousFinalCard` sibling box in `BracketView.tsx` (the real Final's own game-tab selector was lifted one level up, into `BracketRounds`, so both cards always agree on which game is currently being entered). 436 tests (up from 417), 19 new. Went through Plan Mode first, including two research passes over the actual Finals-scoring/roster-mutation code before designing anything, and two genuine implementation-time corrections to the approved plan were found and explained to the organiser rather than silently applied: the `finalComplete` gate above, and the grand-final exclusion. Live-verified end-to-end: flagged a game anonymous, entered placeholder scores (visible live, real finalists' own total correctly excluding them), filled the rest of the Final normally, confirmed "Connect & reveal" only appeared once every game was filled, and confirmed connecting merged the score into the real finalist's total and revealed the mapping. See "Anonymous Finals matches, v1" in `HANDOFF_LOG.md`.
34. ~~Double-elimination: round-numbering mismatch + losers-bracket round delay~~ — done (2026-09-19), two bugs found by reviewing a real production tournament (`1789733102449`) directly: (1) box headers show continuous `roundNum` (e.g. "Round 11") since the earlier "merge LB into WB" redesign, but future-round placeholder content ("Room 6A, Rank 1", "Dropped from WB Round 6") still cited a completely separate WB/LB-specific counter — an oversight from that redesign, which changed the box header but never updated `bracketRoundLabels()` (`bracket.ts`), the actual source of both. Fixed by sourcing both from `round.roundNum` directly; also fixed `FollowBanner`'s identical "out in WB Round N" text for free, since it reads the same function. (2) `sharedFinalDoubleEliminationBracketPhase`'s losers-bracket (LB) scheduling loop (`double-elimination.ts`) could defer creating an LB round indefinitely whenever the pending pool of drops was too small for the room-size-aware target math to find a real cut — confirmed on the real tournament, 3 consecutive winners-bracket (WB) rounds played before the first LB round. Organiser's explicit constraint: at most 2 WB rounds before an LB round, and the forced round must make a genuine cut (not just let everyone through), even in a smaller-than-normal room. Fixed with a new `MAX_WB_ROUNDS_BEFORE_LB = 2` cap and a `forcedLosersSurvivorTarget()` helper that relaxes the room-size floor for just the one forced computation, letting the same geometric-decay math propose a real intermediate cut instead of either no cut or an overly-aggressive jump straight to the final target. Verified independently of the unit tests too: ported the real target-computation logic into a standalone script and ran it against 600 synthetic scenarios (4 configs × 150 seed totals) plus the real 19-player case — zero invariant violations. 441 tests (up from 436), 5 new. Live-verified against the real tournament's own persisted (pre-fix) data — confirmed the round-numbering fix applies retroactively to any already-generated tournament (a pure display-time computation) — and against a freshly-generated 19-player tournament locally, confirming the LB-cadence fix (which only affects newly-generated schedules, not already-persisted ones) actually produces the intended "Round 3, Round 4, LB" cadence instead of "Round 3, Round 4, Round 5, LB." See "Double-elimination: round-numbering mismatch + losers-bracket round delay" in `HANDOFF_LOG.md`.
35. ~~Manual overrides, pre-start only~~ — done (2026-09-19), `ROADMAP.md` item 4: a new "Advanced round overrides (optional)" `<details>` panel in Setup lets the organiser, pre-start only, override (a) qualification-table/Swiss total round count (`qualRoundsOverride`/`swissRoundsOverride`, exposing an existing-but-hardcoded config value), (b) single-elimination/double-elimination-shared-final's WB elimination-round survivor-count curve as an explicit ordered list (`eliminationRoundTargets`, e.g. "24,12,6" — the list's own length is the round-count override for these two formats, sidestepping the "how do you address a round that doesn't exist yet" problem), and (c) a fixed reseed mode (`diversity`/`balance`/`random`) for a specific WB elimination round (`eliminationSeedingOverrides`, index-aligned with the targets list, only valid once that list is set). Scope was settled via 4 `AskUserQuestion` design forks (seeding override limited to `tieredSeed`/`tieredBracketSeed` formats; addressing via an ordered target list; seeding override means a fixed weight pair, not a menu of algorithms; round-count override limited to the 4 formats where it's already a tunable parameter, excluding double-elimination race and Kings Valley). A real architectural discovery mid-build, surfaced and resolved via a 5th `AskUserQuestion`: double-elimination's `tieredBracketSeed` fires once per *target* round and can pool drops from multiple WB source rounds, so the seeding-weight override only applies to a WB round's own WB-to-WB continuation, never its LB-bound drop (which stays on the automatic taper always) — confirmed structurally in tests. `'random'` mode uses a deterministically-seeded shuffle (keyed on round identity + candidate set) rather than real entropy, keeping `advanceTournamentRound` a pure function of state, matching every other transition in the app. 473 tests (up from 441), 32 new, including a hand-derived scenario proving `'diversity'`/`'balance'` produce opposite room assignments for the same input. Live-verified end-to-end via `pnpm dev`: generated a 37-player single-elimination tournament with `eliminationRoundTargets: "32,24,16"` and confirmed the Schedule Preview showed exactly 3 explicit elimination rounds (down from the automatic 4-round curve) followed by Semis and Final; confirmed the panel's field set changes correctly across qual-table/Swiss/single-elimination/double-elimination-shared-final/Kings Valley. See "Manual overrides, pre-start only" in `HANDOFF_LOG.md`.
36. ~~Fix: stale "Scored anonymously below" wording + no single-click "save and start a new tournament" action~~ — done (2026-09-19), two small organiser-reported issues. (1) The real Final's own box still pointed scorers "below" toward the `AnonymousFinalCard`, but that card now renders beside the Final in the same horizontal flex row (a leftover from before the layout changed) — reworded to "Scored anonymously — see side panel →". (2) Investigated live against tournament `1789733102449`/`1789761899543` directly: no bug was hiding a button — "💾 Save to Archive"/"↺ Reset" are the only two bottom buttons that have ever existed, for every tournament, unconditionally. The real gap: "↺ Reset" deliberately only clears progress while keeping the same roster/schedule (so an organiser can re-run an identical tournament), and getting to a blank Setup form needed a second, disconnected step ("↺ Clear roster") nothing linked to "starting fresh." Organiser confirmed (via `AskUserQuestion`) a new dedicated button was the right fix, not changing Reset's existing behavior: "🏁 Save & Start New Tournament" (`RunningAdmin.tsx`) reuses Reset's own smart save-prompt UX, then calls `resetRoster()` (already a superset of `resetTournamentState()`'s own resets). Found and fixed along the way: `resetRoster()` never cleared `roomHistory`/`anonymousFinalists`, a pre-existing gap (also affecting the existing "Clear roster" button) that would have undercut the new button's own promise of a clean slate — a reused player name could have picked up stale rematch-avoidance pressure, or a stale placeholder-alias mapping could have lingered into a new tournament's own Final. 474 tests (up from 473), 1 new (`resetRoster` had zero dedicated coverage before this). Live-verified via `pnpm dev`: played an FFA-individual tournament through to the Final, flagged a game anonymous, confirmed the new wording and the side-by-side layout visually; then, mid-tournament with unsaved changes, clicked the new button, confirmed the "Unsaved tournament" modal's three actions, confirmed "Save & start new" both wrote an archive snapshot (verified via the Archive tab) and landed on a genuinely blank Setup form. See "Fix: stale anonymous-panel wording + missing 'save and start a new tournament' action" in `HANDOFF_LOG.md`.
37. ~~Fix: elimination-target plateau validation + clearer seeding-override copy~~ — done (2026-09-19), two more small organiser-reported issues found while testing "Manual overrides, pre-start only" (#35). (1) `16,16,16,14,12,10,8` was rejected by a "must strictly decrease" check even though an early round can legitimately eliminate no one — traced downstream (`single-elimination.ts`/`double-elimination.ts`) to confirm strict decrease is a validation-layer-only assumption, not relied on for any array indexing or bracket-size math when `explicitTargets` is supplied, then relaxed the check to reject only an actual increase. (2) The seeding-overrides field's hint didn't exhaustively name or explain its three options — added explicit copy for `diversity`/`balance`/`random` plus "blank = automatic," reusing the existing code-comment descriptions in `seeding.ts`/`double-elimination.ts`. 475 tests (up from 474), 1 net new (one old test replaced, one added). Not live-verified in-browser — admin login has no dev-mode bypass (see "Deliberately parked design questions" #7) — verified via the domain suite (exercises `generateTournament` end-to-end) plus lint/format/type-check. See "Fix: elimination-target plateau validation + clearer seeding-override copy" in `HANDOFF_LOG.md`.
38. ~~Positional-points scoring~~ — done (2026-09-20), `ROADMAP.md` item 5: a second, organiser-selectable scoring system alongside Fair Points — a fixed rank-to-points table (organiser-configurable per tournament, e.g. "10,8,6,5,4,3,2,1"), summed across counted rounds rather than averaged, scoped to qual-table/Swiss/Group-Stage standings only (Semis/Final have no rank-based scoring today and were left out, not half-built). Scope settled via 3 `AskUserQuestion` design forks with the organiser (configurable table vs. fixed constant; qualification-only vs. also Semis/Final; sum vs. average). A real correctness risk was found and resolved during research, before any code was written: Fair Points is lower-is-better while positional points is higher-is-better, and every consumer of the standings' cumulative value assumes a specific direction — traced all of them and found only two places actually hard-code it (`materializeStandings`'s own sort, and `computeGroupStageAdvancement`'s cross-group tier sort), fixed both via one shared `compareStandingValue()` helper; everything else (dense ranking, tie-cutoff detection) turned out to already be direction-agnostic. Built in 4 checked-in stages: domain types + validation (`generation.ts` rejects a table that increases rank-to-rank, is too short for the format's largest room, or is set without a pooling phase to accumulate into), the scoring engine itself (`advancement.ts`), Setup UI (the previously-disabled "Scoring system" dropdown is now live, gated on `poolingPhase !== 'none'`), and display formatting across `TournamentStandings.tsx`/`RankingsView.tsx`/`BracketView.tsx`'s tie banners/`RunningAdminStatus.tsx` (a small incidental fix folded in here: `RankingsView.tsx`'s Fair Points display moved from 3 to 5 decimals, matching the other two views' existing precision). 492 tests (up from 475), 17 new. Not live-verified in-browser — same admin-login constraint as #37 — verified via the domain suite plus lint/format/type-check on every changed file. See "Positional-points scoring" in `HANDOFF_LOG.md`.
39. ~~Pre-published, fixed multi-round draws~~ — done (2026-09-20), `ROADMAP.md` item 6: a second, organiser-selectable draw-publication mode for qual-table/Swiss (`drawPublication: 'adaptive' | 'fixed'`, default adaptive) — a fixed tournament's entire room schedule is computed once at generation time from roster order alone, with no live-result dependency ever, instead of the live `tieredSeed`/`swissFoldPair` reseed every other tournament uses. Scope settled via two research passes (an Explore agent, then a dedicated Plan agent) plus 5 `AskUserQuestion` design forks total across this session's scoping and review. Swiss reuses `circleMethodSchedule` (the same primitive Group Stage's own round-robin already uses) directly, guaranteeing zero repeat pairings; qual-table (room size can exceed 2) needed a new greedy wave-based algorithm (`fixed-draws.ts`, new file) reusing `assignWaveToRooms` — the exact same room-choice primitive `tieredSeed` itself uses in production — with every candidate's tier rank held constant, matching (not exceeding) `tieredSeed`'s own accepted quality bar. A critical architectural fact was found and verified directly before designing anything: `state.assignments[roundIndex]` being non-empty is used elsewhere as the "this round has been reached" signal (`rankings.ts`'s `lastAssignedRound`, `BracketView.tsx`'s `PlaceholderRound` gate) — so the fixed schedule is stored per-round on a new `TournamentRound.fixedRoomAssignments` field instead, mirroring Group Stage's own `matches`/`groupByes` precedent exactly, leaving `state.assignments` empty for a fixed round until it's actually reached. Two further real findings, each resolved via `AskUserQuestion` rather than assumed: (1) `rebuildFutureRounds()` (what a reserve-join calls) is a guaranteed no-op for qual-table/Swiss today, so a reserve joining a fixed-draw tournament would silently NOT be incorporated into already-computed future rounds without new regeneration code — resolved by blocking reserves outright for fixed-draw tournaments (new `'fixed-draw'` reason on `addReserveUnit`), rather than building that regeneration path; (2) Bracket now reveals the real room assignments for future fixed-draw rounds (not the generic TBD-style placeholder), since publishing a draw upfront is the entire point of the feature. Built in 4 checked-in stages: domain types + the new algorithm (fully unit-tested in isolation, including two hand-traced examples), `generation.ts`/`schedule-generation.ts` wiring (validation, round-0 assignment, bulk `roomHistory`/`poolingByeCounts` fold-in so the live transition out of the pooling phase still avoids rematches against the fixed rounds), the `transitions.ts` skip-branch (proven via a deliberately non-tiered pairing that only the skip-branch could produce) plus the reserve guard, and Setup UI/Bracket reveal. 509 tests (up from 492), 17 new. **A real, explicitly deferred gap, not silently dropped**: a removed/swapped player during a fixed-draw tournament can leave a stale name in an already-published future round — the same class of bug Group Stage had before `patchFutureGroupStageRounds` was built — confirmed with the organiser as an accepted v1 limitation (see "What is NOT yet built" below) rather than building a second regeneration mechanism in the same pass. Not live-verified in-browser — same admin-login constraint as #37/#38 — verified via the domain suite plus lint/format/type-check on every changed file. See "Pre-published, fixed multi-round draws" in `HANDOFF_LOG.md`.
40. ~~Waterfall / rank-band bracket phase~~ — done (2026-09-21), `ROADMAP.md` items 7 + 8 merged into one project after a reverted first attempt at item 7 alone: an organiser-authored, generation-time-only `waterfall-bracket` schedule logic (a `ROUNDS:`/`ROUTES:` text mini-language parsed by the new `waterfall-bracket.ts`) where each room's finishers split by rank into bands routed to arbitrary, possibly non-adjacent, destination rounds — see "Game formats & tournament structure" above. Reuses `state.pendingBracketSeeds` unchanged; new `isWaterfall`/`waterfallRoutes`/`customLabel` round fields; `advanceWaterfallBracket` + a shared `seedRoundFromPendingPool` extraction in `transitions.ts`; rank-precise future-round projection in `bracket.ts`; reserves blocked. Built in 6 checked-in stages, 554 tests (up from 509), 45 new, live-verified against the real spreadsheet example on a 32-player roster. **Known, live-verified, deliberately-unfixed characteristic**: Rankings can transiently show a still-alive unit as "eliminated" for one round until its destination round is reached (same timing as double-elimination's deferred-LB case). Not built, recorded in `ROADMAP.md`'s appendix: Kings Valley early-exit, auto-derived waterfall brackets, no elasticity for a mid-tournament withdrawal. See "Waterfall / rank-band bracket phase" in `HANDOFF_LOG.md`.
41. ~~Seeding system explainer + findings~~ — done (2026-09-21), **no app code changed**: a private, community-facing Artifact page ("Room Seeding, Explained", https://claude.ai/artifact/1DizdHtzELmbDmBt7jwnGL, owner must share it before anyone else can open it) explaining the diversity/balance room-seeding blend, with a live 32-player, 4-round simulator built on the real `assignWaveToRooms` cost function. Real findings worth knowing (full account in `HANDOFF_LOG.md`, "Seeding system explainer + seeding-behavior findings"): the balance term is frequently an exact tie by structure (equal-tier waves; any 2-room destination), so the automatic taper behaves almost identically to pure `diversity` in common bracket shapes; and a pure `balance` override (diversityWeight exactly 0) ignores repeat history entirely rather than merely weighting it down. No seeding constant was changed; whether to retune the taper is an open question, not a recommendation. Two sketched-only ideas (hard rematch cooldown, form-aware balance) are parked in `ROADMAP.md`; region grouping was ruled out by the organiser.

---

## One important principle
The app's core value is handling "uncomfortable" registration numbers automatically. Whatever the player count — 31, 37, 43, 53 — the app should calculate the optimal room distribution, advancement numbers, and lucky loser count without the organiser needing to think about it. When testing new features, always test with non-round numbers first.
